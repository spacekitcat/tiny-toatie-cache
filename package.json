{
  "name": "tiny-toatie-cache",
  "version": "0.1.0",
  "description": "Prototype cache system for sliding window lookup operations",
  "main": "index.js",
  "repository": "https://github.com/spacekitcat/tiny-toatie-cache",
  "author": "Lisa Burton",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "babel-jest": "^24.0.0",
    "jest": "^24.0.0"
  },
  "scripts": {
    "test": "jest --coverage",
    "prebuild": "babel src --out-dir lib --source-maps",
    "build": "npm run test",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "cloakroom-smart-buffer-proxy": "^2.0.6"
  }
}
